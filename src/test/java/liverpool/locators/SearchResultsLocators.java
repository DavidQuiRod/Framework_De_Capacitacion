package liverpool.locators;

import org.openqa.selenium.By;

public class SearchResultsLocators {

    // Heurística: contenedor de resultados / grid. Si cambia, se ajusta.
    public static final By RESULTS_CONTAINER = By.cssSelector("main");
}
